#include<stdio.h>
int main()
{
	int length;
	int breadth;
	printf("Enter the length =");
	scanf("%d",&length);
	printf("Enter the breadth =");
	scanf("%d",&breadth);
	int area;
	area = length * breadth ;
	printf("The area is = %d",area);
	
}
